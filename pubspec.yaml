name: box_manager
description: Simple box & item manager (web+mobile)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive_flutter: ^1.1.0
  csv: ^6.0.0
  qr_flutter: ^4.1.0
  google_sign_in: ^6.2.1
  googleapis: ^12.0.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
